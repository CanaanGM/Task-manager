# Tasks-Manager
## back-end for a nodejs server that manages my tasks

- keeps track of tasks related to users
- o(*￣▽￣*)ブ

## Features

- mongo as the db , node js as the server and jest as the funny test framework!

## Installation

Install the dependencies and devDependencies and start the server.

```sh
cd task-manager
npm i
npm start
```

## Development

```sh
npm test
```

## Docker

To be added, planning to add it with the DB as a compose file, whenever i have time
